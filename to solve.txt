1、（已修改）对于sgls，当节点被删除后，cluster中对应的节点（记录的顶点位置）应该相应删除。而新节点应该添加到当前那个cluster？
2. 对于sgls，assign节点应在assign时被写入文件，另外，如果sample graph中节点被删除，其应转变为assign节点。

3. assign 节点的处理，内存中保存所有assigned vertex及其AC。如果assigned vertex被采样则标记但不删除，因为其有可能再一次被unsampled。
//将assign的顶点及其partition保存在manager中，不保存AC。manger保存所有assign顶点信息，不在cluster中保存了。如果sampled，则标记。在最终阶段assign节点不在重新评估，可能cut会变大，但均值不会太高，因为绝大多数顶点会被采样。

4.在最后阶段，对每个分区中的顶点，检查是否在cluster中，如果不在是否在assign列表中，来确定最终划分。
5. （已解决，在删除前保存所有变化节点的划分）当划分中节点通过采样发生变化，检查是否重新划分中，删除节点如何获得划分。
6.检查MergeLeafofNode等划分编号正确性